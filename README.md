# ระบบจองคิวออนไลน์ — รพ.สต.บ้านหนองครกใต้

หน้าจองเปิดในแอป LINE (LIFF) จากริชเมนู · **แยกหมวดตามแผนกตั้งแต่หน้าแรก** · **ไม่จำกัดจำนวนคิว**
หลังบ้านใช้ Google Sheets + Apps Script + LINE Messaging API — ไม่มีค่าเซิร์ฟเวอร์รายเดือน

## ไฟล์ในโปรเจ็ค

```
line-oa-arm/
├── config.js               ตั้งค่าการเชื่อมต่อ 3 บรรทัด (LIFF_ID · API_URL · DEMO)
├── assets/theme.css        ธีมเขียวมิ้น–ขาว ใช้ร่วมกันทุกหน้า
├── assets/date-th.js       วันเวลาแบบไทย (Asia/Bangkok + พ.ศ.) ใช้ร่วมกันทุกหน้า
├── liff/index.html         หน้าจองสำหรับประชาชน
├── staff/index.html        หน้าเจ้าหน้าที่ 4 แท็บ (คิว · นัดล่วงหน้า · เสร็จสิ้น · สรุปยอด)
├── gas/Code.gs          ⭐ หลังบ้าน + ตารางเวลา/บริการทั้งหมด — วางใน Google Apps Script
├── gas/appsscript.json     ตั้งค่าโปรเจ็ค Apps Script
├── tests/check.js          ชุดตรวจสอบตรรกะ — รัน `node tests/check.js` ก่อนอัปทุกครั้ง
├── vercel.json             ตั้งค่าการอัปขึ้นเว็บและ security headers
├── robots.txt              กันหน้าเจ้าหน้าที่ถูกเก็บเข้าดัชนีค้นหา
└── prototype/index.html    ตัวอย่างหน้าจอไว้ดูภาพรวม (ไม่เกี่ยวกับระบบจริง)
```

## ตรวจสอบก่อนอัปขึ้นเว็บ

```bash
node tests/check.js
```

ตรวจ 68 จุด — การกันสูตรฝังในชีต · checksum เลขบัตร · การล็อกรหัสเจ้าหน้าที่ · ปฏิทินไทย
และการเปิดเผยข้อมูล ใช้เวลาไม่ถึงวินาที ไม่ต่ออินเทอร์เน็ต ไม่แตะข้อมูลจริง
ถ้าขึ้น "ไม่ผ่าน" แม้แต่ข้อเดียว อย่าเพิ่ง deploy

## แผนกที่เปิดให้จอง

| แผนก | เลขคิว | วันเปิด | ช่วงเวลา |
|---|---|---|---|
| แพทย์แผนไทย | ท-001 | จันทร์–ศุกร์ | 09–10 · 10–11 · 11–12 · 13:30–14:30 · 15–16 |
| ทันตกรรม | ฟ-001 | อังคาร · พฤหัสบดี · ศุกร์ ⚠️ *รอยืนยัน* | 09–10 · 10–11 · 11–12 · 14–15 · 15–16 |

แก้วันเปิด · ช่วงเวลา · รายการบริการ ได้ที่ตัวแปร `DEPTS` หัวไฟล์ [`gas/Code.gs`](gas/Code.gs) **ที่เดียว**
หน้าเว็บดึงค่าจากเซิร์ฟเวอร์ตอนเปิด ไม่ต้องแก้ `config.js` ให้ตรงกันอีก (ค่าใน `config.js` เป็นค่าสำรองเฉพาะโหมด DEMO)
แก้เสร็จอย่าลืม **Deploy เวอร์ชันใหม่** ไม่งั้นหน้าเว็บยังเห็นของเก่า

---

# สิ่งที่ต้องทำ ทีละขั้น

ทำตามลำดับ ใช้เวลารวมประมาณ 1 ชั่วโมง ทุกอย่างฟรี

## ☐ ขั้นที่ 0 · ลองเปิดดูก่อน (2 นาที)

ดับเบิลคลิก `liff/index.html` และ `staff/index.html` เปิดในเบราว์เซอร์ได้เลย
ตอนนี้ `config.js` ตั้ง `DEMO: true` ไว้ — ใช้ข้อมูลตัวอย่าง ไม่บันทึกอะไรจริง
ถ้าหน้าตาหรือรายการบริการยังไม่ถูก แก้ที่ `config.js` ก่อน แล้วค่อยทำขั้นถัดไป

## ☐ ขั้นที่ 1 · สร้างฐานข้อมูล (Google Sheets + Apps Script)

1. สร้าง Google Sheet ใหม่ ตั้งชื่อ `ฐานข้อมูลคิว รพ.สต.บ้านหนองครกใต้`
2. เมนู **ส่วนขยาย → Apps Script**
3. ลบโค้ดเดิมทิ้ง แล้ววางเนื้อหาทั้งหมดจาก [`gas/Code.gs`](gas/Code.gs) ลงไป
4. กด 💾 บันทึก แล้วเปิดค้างไว้ — จะกลับมาที่หน้านี้อีกในขั้นที่ 4

> 🔁 **ทุกครั้งที่อัปเดตโค้ดในอนาคต** ให้วางทับ → บันทึก → **รัน `setup()` อีกครั้ง** → Deploy เวอร์ชันใหม่
> `setup()` จะเติมคอลัมน์ใหม่ให้ชีตเดิมโดยไม่แตะข้อมูลเก่า

## ☐ ขั้นที่ 2 · เปิด Messaging API บน LINE OA เดิม

`LINE Official Account Manager` → เลือกบัญชี → **ตั้งค่า** → **Messaging API** → **ใช้งาน Messaging API**

- เลือกหรือสร้าง **Provider** — ตั้งชื่อเป็นชื่อหน่วยงาน
  > ⚠️ **จำ Provider ตัวนี้ไว้** ขั้นที่ 3 ต้องใช้ตัวเดียวกัน ถ้าใช้คนละตัว LIFF จะดึงข้อมูลผู้จองไม่ได้
- ทำเสร็จจะได้ **Channel secret**

จากนั้นไปที่ `LINE Developers Console` → Provider เดิม → ช่อง Messaging API → แท็บ **Messaging API**

- หา **Channel access token (long-lived)** → กด **Issue** → คัดลอกเก็บไว้ ➊
- ที่หน้าเดียวกัน ปิด **Auto-reply messages** และ **Greeting messages** (ถ้าไม่ต้องการให้บอทตอบอัตโนมัติทับ)

## ☐ ขั้นที่ 3 · สร้าง LIFF (หน้าจอง)

`LINE Developers Console` → **Provider เดิม** → **Create a new channel** → **LINE Login**

- Channel name: `จองคิว รพ.สต.บ้านหนองครกใต้`
- App types: ติ๊ก **Web app**
- สร้างเสร็จ → แท็บ **Basic settings** → คัดลอก **Channel ID** เก็บไว้ ➋
- ไปแท็บ **LIFF** → **Add**
  - Endpoint URL: ใส่ `https://example.com` ไปก่อน (แก้ทีหลังในขั้นที่ 6)
  - Size: **Full**
  - Scopes: ติ๊ก **profile** และ **openid**
- จะได้ **LIFF ID** หน้าตา `2001234567-AbCdEfGh` เก็บไว้ ➌

## ☐ ขั้นที่ 4 · ต่อ Apps Script เข้ากับ LINE

กลับไปหน้า Apps Script แล้วทำ 3 อย่าง

**4.1 ใส่ค่าลับ** — ⚙️ **Project Settings** → เลื่อนลงหา **Script Properties** → **Add script property**

| Property | Value |
|---|---|
| `CHANNEL_ACCESS_TOKEN` | ➊ จากขั้นที่ 2 |
| `LOGIN_CHANNEL_ID` | ➋ Channel ID ของ **LINE Login** channel (ไม่ใช่ของ Messaging API) |
| `STAFF_KEY` | ตั้งเอง **อย่างน้อย 12 ตัวอักษร** ผสมตัวเลขและอักขระพิเศษ |

> 🔑 `API_URL` เป็นข้อมูลสาธารณะ (อยู่ใน `config.js` ที่ใครก็เปิดอ่านได้)
> `STAFF_KEY` จึงเป็นด่านเดียวที่กั้นชื่อ เบอร์โทร และอาการของคนไข้ทั้งหมด **ห้ามตั้งสั้นหรือเดาง่าย**
> ระบบจะหน่วงเวลา 1.2 วินาทีเมื่อใส่ผิด และล็อก 15 นาทีเมื่อผิดติดกัน 5 ครั้ง
> ถ้าเจ้าหน้าที่โดนล็อกไปด้วย ให้รันฟังก์ชัน `unlockStaff()` ในตัวแก้ไขสคริปต์

> 🔒 ค่าพวกนี้เก็บที่นี่ที่เดียว **ห้ามใส่ใน `config.js`** เพราะไฟล์นั้นใครก็เปิดอ่านได้จากเบราว์เซอร์
> ใครได้ `CHANNEL_ACCESS_TOKEN` ไป จะส่งข้อความในนามหน่วยงานได้ทันที

**4.2 รัน `setup()` หนึ่งครั้ง** — เลือกฟังก์ชัน `setup` ที่แถบด้านบน → กด **Run**
Google จะขออนุญาตเข้าถึงข้อมูล → **Review permissions** → เลือกบัญชี → **Advanced** → **Go to … (unsafe)** → **Allow**
(ขึ้นว่า unsafe เพราะเป็นสคริปต์ที่เขียนเอง ไม่ได้ผ่านการรับรองจาก Google — ปกติ)

เสร็จแล้วจะเห็นชีต `bookings` กับ `patients` โผล่ขึ้นมา และตั้งเวลาส่งแจ้งเตือน 19:00 ทุกวันให้อัตโนมัติ

**4.3 Deploy** — **Deploy** → **New deployment** → ⚙️ เลือก **Web app**

- Execute as: **Me**
- Who has access: **Anyone**
- กด **Deploy** → คัดลอก **Web app URL** ที่ลงท้าย `/exec` เก็บไว้ ➍

## ☐ ขั้นที่ 5 · อัปหน้าเว็บขึ้นอินเทอร์เน็ต

LIFF ต้องเป็น `https://` เปิดจากไฟล์ในเครื่องไม่ได้ · โปรเจ็คนี้เตรียม `vercel.json` ไว้ให้แล้ว

### วิธีที่ 1 — Vercel (แนะนำ)

เปิด Terminal แล้วรัน 2 คำสั่งนี้ในโฟลเดอร์โปรเจ็ค

```bash
cd ~/Desktop/line-oa-arm
npx vercel --prod
```

ครั้งแรกจะให้ล็อกอิน (อีเมลหรือ GitHub) แล้วถามไม่กี่ข้อ — กด Enter ผ่านได้หมด

| คำถาม | ตอบ |
|---|---|
| Set up and deploy? | `y` |
| Which scope? | เลือกบัญชีตัวเอง |
| Link to existing project? | `n` |
| Project name? | `line-oa-arm` (หรือกด Enter) |
| In which directory is your code located? | `./` |
| Want to modify settings? | `n` |

เสร็จแล้วจะได้ URL เช่น `https://line-oa-arm.vercel.app`

- หน้าจอง → `https://line-oa-arm.vercel.app/liff/` (เปิดโดเมนเปล่าก็เด้งมาหน้านี้)
- หน้าเจ้าหน้าที่ → `https://line-oa-arm.vercel.app/staff/`
- ตัวอย่างหน้าจอ → `https://line-oa-arm.vercel.app/prototype/`

**แก้ไฟล์ทีหลังแล้วอัปใหม่** — รัน `npx vercel --prod` ซ้ำในโฟลเดอร์เดิม ได้ URL เดิม
(เช่น ตอนแก้ `config.js` ในขั้นที่ 6 ต้องรันซ้ำอีกครั้ง)

> `vercel.json` ตั้งไว้ให้แล้ว: เปิดโดเมนเปล่าเด้งไปหน้าจอง, ไม่ให้ Google เก็บหน้าเจ้าหน้าที่ไปทำดัชนี,
> และไม่ cache `config.js` เพื่อให้แก้ค่าแล้วมีผลทันที · `.vercelignore` กันไม่ให้อัปโฟลเดอร์ `gas/` ขึ้นเว็บ

### วิธีที่ 2 — Netlify Drop (ไม่ต้องใช้ Terminal)

เปิด `app.netlify.com/drop` แล้วลากโฟลเดอร์ `line-oa-arm` ทั้งโฟลเดอร์ไปวาง ได้ URL ทันที
ข้อเสีย: อัปใหม่ทุกครั้งต้องลากใหม่ และได้ URL ใหม่ถ้าไม่ผูกบัญชี

### วิธีที่ 3 — GitHub + Vercel

push ขึ้น GitHub แล้วกด Import ที่ `vercel.com/new` — หลังจากนั้นแก้โค้ดแล้ว push
Vercel จะ deploy ให้เองอัตโนมัติ เหมาะถ้าจะดูแลระยะยาว

## ☐ ขั้นที่ 6 · เชื่อมทุกอย่างเข้าด้วยกัน

**6.1** แก้ [`config.js`](config.js) 3 บรรทัด แล้วอัปโหลดใหม่

```js
LIFF_ID: '2001234567-AbCdEfGh',                          // ➌
API_URL: 'https://script.google.com/macros/s/..../exec', // ➍
DEMO: false,                                             // ← เปลี่ยนเป็น false
```

**6.2** กลับไป `LINE Developers Console` → LIFF → แก้ **Endpoint URL** เป็น `https://xxxx.netlify.app/liff/`

## ☐ ขั้นที่ 7 · เปลี่ยนปุ่มในริชเมนู

`LINE Official Account Manager` → **ริชเมนู** → ปุ่ม **จองคิวรับบริการ**
เปลี่ยนจากลิงก์ Google Form เป็น

```
https://liff.line.me/2001234567-AbCdEfGh
```

(ใช้ LIFF ID ➌ ของตัวเอง) แล้วกดบันทึก

## ☐ ขั้นที่ 8 · ทดสอบจริง

1. เปิดแชท LINE OA ด้วยมือถือตัวเอง กดปุ่ม **จองคิวรับบริการ**
2. จองคิวทดสอบ 1 คิว → ต้องได้บัตรคิวเด้งเข้าแชท
3. เปิด Google Sheet → ต้องเห็นแถวใหม่ในชีต `bookings`
4. เปิด `https://xxxx.netlify.app/staff/` ใส่ `STAFF_KEY` → ต้องเห็นคิวที่เพิ่งจอง
5. กด **เรียกคิว** → มือถือต้องได้ข้อความ "ถึงคิวของท่านแล้ว"
6. ลบแถวทดสอบออกจาก Sheet

## ☐ ขั้นที่ 9 · ยื่นขอบัญชีรับรอง (ทำคู่ขนานได้)

ตอนนี้ LINE OA ยังขึ้นแถบ *"บัญชีนี้ไม่ได้เป็นบัญชีรับรอง"* — ระบบนี้ขอเลขบัตรประชาชน
หน่วยงานจึงควรยื่นขอ **บัญชีรับรอง (โล่น้ำเงิน)** ที่ OA Manager → ตั้งค่า → ข้อมูลบัญชี
ใช้เอกสารหน่วยงาน ฟรี ประมาณ 10 วันทำการ

---

## ข้อมูลที่ระบบเก็บ

**ชีต `bookings`** — 1 แถวต่อ 1 คิว
`id · createdAt · userId · displayName · dept · service · date · slot · queueNo · name · idCard · tel · right · note · status · calledAt · updatedAt`

**ชีต `patients`** — 1 แถวต่อ 1 คน ใช้เติมฟอร์มให้อัตโนมัติในการจองครั้งถัดไป
`userId · name · idCard · tel · right · consentAt · updatedAt`

`status` มีค่า `booked` (รอเรียก) · `checkin` · `done` · `noshow` · `cancelled`
ไม่มีการลบแถว ยกเลิกคิวคือเปลี่ยน status เป็น `cancelled` เพื่อให้ตรวจย้อนหลังได้

## หน้าเจ้าหน้าที่ทำอะไรได้บ้าง

เปิดที่ `/staff/` ใส่ `STAFF_KEY` ครั้งเดียว เบราว์เซอร์จำไว้ให้

| แท็บ | ใช้ทำอะไร |
|---|---|
| **คิวรับบริการ** | คิวของวันที่เลือก เฉพาะที่ยังรอเรียกและเช็คอินแล้ว · กดเรียกคิว เช็คอิน ไม่มา · กรองตามแผนก |
| **นัดล่วงหน้า** | คิวที่จองไว้ล่วงหน้าทั้งหมด จัดกลุ่มตามวัน · รายการที่เจ้าหน้าที่ยังไม่เคยเห็นติดป้าย **ใหม่** |
| **เสร็จสิ้น** | เคสที่ให้บริการจบแล้วของวันนั้น **แยกตามแผนก** และมีหมวดไม่มาตามนัด / ยกเลิกต่อท้าย |
| **สรุปยอด** | สรุปรายวันแยกแผนกและแยกบริการ + ตารางย้อนหลัง 7 หรือ 30 วัน |

**การแจ้งเตือนคิวจองล่วงหน้า**

- หน้าจอรีเฟรชเองทุก 60 วินาที และทุกครั้งที่กลับมาที่แท็บเบราว์เซอร์นี้ ไม่ต้องกดรีเฟรชเอง
- มีคิวใหม่ → ขึ้นแถบสีเหลืองบนสุด บอกจำนวนและรายการที่ใกล้ที่สุด + ตัวเลขสีแดงบนแท็บ *นัดล่วงหน้า*
- มีเสียงเตือนสั้น ๆ เมื่อคิวใหม่เข้ามาระหว่างเปิดหน้าไว้ ปิดได้ที่ปุ่ม 🔔 มุมขวาบน
- "เคยเห็นแล้ว" จำแยกตามเครื่อง เปิดแท็บ *นัดล่วงหน้า* ค้างไว้ประมาณ 1 วินาที ถือว่ารับทราบแล้ว

> คิวที่ยังไม่ถึงวันนัด กดเปลี่ยนสถานะไม่ได้ (ดูอย่างเดียว) เพื่อกันเผลอเช็คอินล่วงหน้า
> พอถึงวันนั้น คิวจะไปโผล่ในแท็บ *คิวรับบริการ* เอง

## เรื่องวันเวลา

- วันที่ทั้งระบบอ้างอิง **เขตเวลาประเทศไทย (Asia/Bangkok)** เสมอ ไม่ว่ามือถือผู้ใช้จะตั้งเขตเวลาอะไรไว้
- คำนวณวันด้วย UTC ล้วนแล้วค่อยแปลงเป็นข้อความไทย เดือนและปีจึงข้ามได้ถูกต้อง (30 ก.ย. → 1 ต.ค., 31 ธ.ค. → 1 ม.ค.) และรองรับปีอธิกสุรทิน
- แสดงปีเป็น พ.ศ.
- แถบเลือกวันเริ่มที่ **วันนี้** และจองวันนี้ได้ถ้ายังไม่เลยเวลาของช่วงนั้น
  ช่วงที่เลยเวลาไปแล้วจะเป็นสีเทากดไม่ได้ · ถ้าต้องการบังคับให้จองล่วงหน้าอย่างน้อย 1 วัน
  เปลี่ยน `ALLOW_SAME_DAY` เป็น `false` ทั้งใน `config.js` และ `gas/Code.gs`

## เรื่องความปลอดภัยที่ทำไว้

- หน้าเว็บส่ง **LINE ID token** มาให้หลังบ้านตรวจกับเซิร์ฟเวอร์ LINE ทุกครั้ง ไม่เชื่อ `userId` ที่ส่งมาจากเบราว์เซอร์
- เลขบัตรประชาชนตรวจ checksum 13 หลักก่อนบันทึก และแสดงผลแบบปิดบังเมื่อเติมฟอร์มครั้งถัดไป
- กันจองซ้ำ: คนเดิม แผนกเดิม วันเดียวกัน จองได้คิวเดียว
- กันจองย้อนหลัง จองเกิน 14 วัน และจองวันที่แผนกไม่เปิด — ตรวจซ้ำที่ฝั่งเซิร์ฟเวอร์ ไม่ได้เชื่อหน้าเว็บอย่างเดียว
- ยกเลิกคิวได้เฉพาะเจ้าของคิวเท่านั้น
- หน้าเจ้าหน้าที่ต้องใช้ `STAFF_KEY`

## การป้องกันที่ใส่ไว้

| ความเสี่ยง | วิธีป้องกัน |
|---|---|
| **สูตรฝังในชีต** — ชื่อโปรไฟล์ LINE ที่ตั้งเป็น `=IMPORTXML(...)` ดูดข้อมูลทั้งชีตออกไปตอนเปิดไฟล์ | ทุกข้อความที่ขึ้นต้นด้วย `=` `+` `-` `@` ถูกบังคับเป็นข้อความล้วนก่อนเขียนลงชีต |
| **เดารหัสผ่านเจ้าหน้าที่** | หน่วงเวลา 1.2 วินาทีทุกครั้งที่ผิด + ล็อก 15 นาทีเมื่อผิด 5 ครั้ง + เทียบรหัสแบบเวลาคงที่ |
| **ปลอมตัวเป็นคนอื่น** | ตรวจ LINE ID token กับเซิร์ฟเวอร์ LINE ทุกคำขอ ไม่เชื่อ `userId` จากเบราว์เซอร์ |
| **ยิง API ตรงด้วยค่าเพี้ยน** | ตรวจแผนก บริการ ช่วงเวลา วันเปิดทำการ วันหยุด และ checksum เลขบัตร ที่ฝั่งเซิร์ฟเวอร์ทั้งหมด |
| **จองรัว ๆ ปั่นคิว** | 1 คนจองได้ไม่เกิน 4 คิวที่ยังไม่ถึงวันนัด และห้ามซ้ำแผนก–วันเดียวกัน |
| **XSS ในหน้าเจ้าหน้าที่** | ข้อมูลผู้ใช้ทุกจุดผ่าน `esc()` หรือใส่ผ่าน `textContent` |
| **เกินโควตา Apps Script** | หน้าเจ้าหน้าที่รวมเหลือคำขอเดียว + แคชฝั่งเซิร์ฟเวอร์ 45 วินาที ลดการอ่านชีตลงราว 8 เท่า |
| **ระบบล่มแบบเงียบ ๆ** | ทุกข้อผิดพลาดขึ้นแถบแดงพร้อมปุ่มลองใหม่ ไม่แสดงว่า "ไม่มีคิว" เวลาต่อเซิร์ฟเวอร์ไม่ได้ |
| **ส่งบัตรคิวไม่สำเร็จแต่บอกว่าสำเร็จ** | ระบบรายงานตามจริง ถ้าส่งไม่ได้จะเตือนให้บันทึกภาพหน้าจอ |
| **ข้อมูลที่กรอกหายเมื่อ token หมดอายุ** | บันทึกร่างไว้ในเบราว์เซอร์อัตโนมัติ และมีปุ่มเข้าสู่ระบบใหม่โดยไม่เสียข้อมูล |
| **แจ้งเตือนซ้ำ** | บันทึก `remindedAt` ทุกครั้งที่ส่ง ถ้า Google รัน trigger ซ้ำจะไม่ส่งซ้ำ |

เพิ่มเติมในรอบตรวจสอบที่สอง

| ความเสี่ยง | วิธีป้องกัน |
|---|---|
| **คนร้ายล็อกเจ้าหน้าที่ออกจากระบบ** ด้วยการจงใจใส่รหัสผิดรัว ๆ | ตรวจรหัสที่ถูกต้องก่อนเสมอ คนที่รหัสถูกจึงเข้าได้ตลอดแม้ระบบกำลังนับรหัสผิดอยู่ |
| **ค่าอย่าง `__proto__` ตอนเลือกแผนก** ทำให้ได้ออบเจกต์แปลกปลอมกลับมา | ค้นแผนกผ่าน `dept_()` ที่ตรวจ `hasOwnProperty` ทั้งหลังบ้านและหน้าเว็บ |
| **วันที่ที่ไม่มีอยู่จริง** เช่น 31 ก.พ. ผ่านการตรวจแล้วถูกเลื่อนเป็น 3 มี.ค. เงียบ ๆ | `isYmd_()` แปลงวันที่กลับมาเทียบกับข้อความเดิมก่อนรับ |
| **ช่องวันที่ว่างทำหน้าเจ้าหน้าที่พังทั้งหน้า** | วันที่ที่ใช้ไม่ได้จะถอยไปใช้วันนี้แทนอัตโนมัติ |
| **หน้าเจ้าหน้าที่ถูกฝังใน iframe เว็บอื่น (clickjacking)** | ตั้ง `X-Frame-Options: DENY` + `frame-ancestors 'none'` + `Cache-Control: no-store` |
| **เรียกคิวซ้ำโดยไม่รู้ตัว** | แสดงป้าย "เรียกแล้ว HH:MM" และเปลี่ยนปุ่มเป็น "เรียกซ้ำ" |
| **คอลัมน์ที่เจ้าหน้าที่เพิ่มเองในชีตถูกล้าง** ตอนระบบอัปเดตข้อมูลคนไข้ | เขียนทับเฉพาะคอลัมน์ของระบบ คอลัมน์อื่นคงค่าเดิม |
| **อ่านหัวตารางซ้ำหลายรอบต่อ 1 คำขอ** | แคชหัวตารางไว้ต่อการรันหนึ่งครั้ง |

เพิ่มเติมในรอบตรวจสอบที่สาม

| ความเสี่ยง | วิธีป้องกัน |
|---|---|
| **วนลูปเข้าสู่ระบบไม่รู้จบ** ถ้า LIFF ลืมติ๊กสิทธิ์ openid — ผู้ใช้เห็นแค่หน้าขาวสลับไปมา | ตรวจว่าได้ ID token จริงก่อน และจำกัดการ login ซ้ำไม่เกิน 2 ครั้ง แล้วบอกตรง ๆ ว่าต้องไปแก้ที่ LINE Developers |
| **จองพร้อมกันหลายคนแล้วค้าง** — เดิมถือล็อกไว้ระหว่างรอ LINE ตอบ ซึ่งกินเวลาเป็นวินาที | ส่งบัตรคิวหลังปล่อยล็อกแล้ว และถ้ารอล็อกไม่ทันจะบอกให้กดยืนยันใหม่ แทนที่จะขึ้น error ที่อ่านไม่รู้เรื่อง |
| **เจ้าหน้าที่ยกเลิกนัดแล้วคนไข้ไม่รู้** จนเดินทางมาถึงแล้วเสียเที่ยว | ส่งข้อความแจ้งเข้า LINE ทันทีพร้อมเหตุผล (ถ้าระบุ) |
| **หน้าจองและหน้าตัวอย่างถูก Google เก็บเข้าดัชนี** ทั้งที่เปิดนอกแอป LINE ก็ใช้ไม่ได้ | ใส่ `X-Robots-Tag: noindex` ทุกหน้า |
| **เลขบัตรที่บันทึกไว้ใช้ไม่ได้** แต่หน้าจอบอกว่า "เว้นว่างไว้ได้" | เปลี่ยนข้อความเป็น "ไม่พบเลขบัตรที่บันทึกไว้ กรุณากรอกใหม่" |

> ⚠️ Apps Script มองไม่เห็น IP ผู้เรียก การนับรหัสผิดจึงนับรวมทั้งระบบ
> แต่คนที่ใส่รหัสถูกจะเข้าได้เสมอ การล็อกจึงกระทบเฉพาะคนที่เดารหัสผิด
> ถ้าเจ้าหน้าที่ลืมรหัสและอยากล้างตัวนับ ให้รัน `unlockStaff()` ในตัวแก้ไขสคริปต์

## Security headers ที่ตั้งไว้

ตั้งใน [`vercel.json`](vercel.json) · ใช้กับทุกหน้า

| Header | ค่า | ป้องกันอะไร |
|---|---|---|
| `Strict-Transport-Security` | Vercel ใส่ให้เอง | บังคับ https ตลอด |
| `X-Content-Type-Options` | `nosniff` | กันเบราว์เซอร์เดาชนิดไฟล์ผิด |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | ไม่ส่ง path ที่มีข้อมูลออกไปเว็บอื่น |
| `Permissions-Policy` | ปิด gps/ไมค์/กล้อง/จ่ายเงิน | หน้าเว็บนี้ไม่ต้องใช้ |
| `X-Frame-Options` | `SAMEORIGIN` | กัน clickjacking |
| `Content-Security-Policy` | `frame-ancestors 'self'; base-uri 'self'; object-src 'none'; form-action 'self'` | กัน clickjacking · กันแอบใส่ `<base>` เปลี่ยนปลายทางลิงก์ทั้งหน้า · ปิด plugin · กันแอบสร้างฟอร์มส่งข้อมูลออกนอก |
| `X-Permitted-Cross-Domain-Policies` | `none` | ปิดช่องทางเก่าของ Flash/PDF |
| `X-Robots-Tag` | `noindex` ทุกหน้า | ไม่ให้ Google เก็บเข้าดัชนี |

**ทำไม CSP ไม่ระบุ `script-src` / `connect-src`**

เพราะโค้ดแอปเขียนอยู่ใน `<script>` ในหน้า และมี `style="..."` อีก 22 จุด
ถ้าจะใส่ต้องเปิด `'unsafe-inline'` ทั้งคู่ ซึ่งเป็นการเปิดรูที่ CSP ตั้งใจจะปิดพอดี — ได้คะแนนสวยขึ้นแต่ป้องกันจริงได้น้อย

และมีกับดักอีกข้อ: การเรียก Apps Script มีการ redirect
`script.google.com` → `script.googleusercontent.com` **CSP ตรวจปลายทางของ redirect ด้วย**
ถ้าใส่ `connect-src` แล้วลืมโดเมนที่สอง การจองคิวจะล้มเหลวทุกครั้งแบบเงียบ ๆ

directive 4 ตัวที่เลือกใช้ **ไม่คุมการโหลดทรัพยากรใด ๆ เลย** จึงไม่มีทางทำให้ระบบพัง
แต่ยังกันการโจมตีที่ใช้ได้จริงได้ (ตรวจแล้วว่าโปรเจ็คนี้ไม่มี `<form>` `<object>` `<base>` `<iframe>` สักจุด)

> ถ้าต้องการ CSP เต็มรูปแบบจริง ๆ ต้องย้ายโค้ดออกจาก `<script>` ไปเป็นไฟล์แยก
> และเปลี่ยน `style="..."` เป็น class ก่อน แล้วต้องทดสอบบนมือถือจริงหลายรอบ

## สิ่งที่ยังไม่ได้ทำ

- ยังไม่ได้ยืนยัน **วันเปิดทำการของทันตกรรม** — ตอนนี้ตั้งไว้ อังคาร/พฤหัส/ศุกร์ ที่ `config.js` และ `gas/Code.gs`
- ยังไม่มีหน้าตั้งวันหยุดราชการแบบกดเลือก — ตอนนี้ใส่เป็นวันที่ในช่อง `HOLIDAYS` ของ `config.js`
- ยังไม่มี QR ในบัตรคิว (เจ้าหน้าที่ค้นด้วยชื่อ/เลขคิวในหน้าเจ้าหน้าที่แทน)
- การแจ้งเตือนคิวใหม่ทำงานเฉพาะตอนเปิดหน้า `/staff/` ค้างไว้ · ยังไม่ส่งเข้า LINE กลุ่มเจ้าหน้าที่
- หน้าเจ้าหน้าที่ยังใช้รหัสผ่านร่วมกันทั้งหน่วยงาน ยังไม่แยกรายบุคคลและไม่มีบันทึกว่าใครกดอะไร
- ข้อมูลเก่าที่บันทึกไว้ก่อนอัปเดตนี้ ยังไม่ผ่านการกันสูตร — ถ้าเคยเปิดใช้จริงแล้ว ควรตรวจชีตย้อนหลังว่ามีช่องไหนขึ้นต้นด้วย `=` หรือไม่
- หน้าเจ้าหน้าที่จำรหัสผ่านไว้ในเบราว์เซอร์เครื่องนั้น เหมาะกับคอมพิวเตอร์ของหน่วยงาน ไม่ควรใช้บนเครื่องสาธารณะ
